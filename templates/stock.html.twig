<!DOCTYPE html>
<html lang="fr">
<head>
    {% include 'navbarservicepreparationsdecommandes.html.twig' %}
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Stocks</title>
    <br>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7f8;
            color: #333;
            margin: 0;
            padding-top: 70px; /* Ajuster pour la navbar fixe */
        }

        /* Style pour la navbar pour qu'elle reste fixée en haut */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background-color: #234b75; /* Couleur de fond pour la navbar */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ajout d'une ombre pour un effet visuel */
        }

        h1 {
            text-align: center;
            color: #315174;
            font-size: 2.5em;
            margin-bottom: 30px;
        }

        table {
            width: 90%;
            margin: 30px auto;
            border-collapse: collapse;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
        }

        thead {
            background-color: #275c96;
            color: white;
        }

        th, td {
            padding: 15px;
            text-align: center;
            border-bottom: 1px solid #e0e0e0;
        }

        th {
            font-weight: bold;
        }

        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tbody tr:hover {
            background-color: #f1f1f1;
        }

        .no-data {
            text-align: center;
            padding: 20px;
            font-size: 1.2em;
            color: #666;
        }
    </style>
</head>
<body>
    <h1>Liste des Stocks</h1>

    <table>
        <thead>
            <tr>
              
               
                <th>Produit</th>
                <th>Quantité</th>
                <th>Entrepôt</th>
                <th>Fournisseur</th>
            </tr>
        </thead>
        <tbody>
            {% for stock in stocks %}
                <tr>
                    <td>{{ stock.idProduit.Nom }}</td> <!-- Assurez-vous que "Nom" est un champ de l'entité Produit -->
                    <td>{{ stock.quantite }}</td>
                   
                    <td>{{ stock.entrepot.nomEntrepot }}</td> <!-- Assurez-vous que "nomEntrepot" est un champ de l'entité Entrepot -->
                    <td>{{ stock.fournisseur.nomFournisseur }}</td> <!-- Assurez-vous que "nomFournisseur" est un champ de l'entité Fournisseur -->
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="no-data">Aucun stock trouvé.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
